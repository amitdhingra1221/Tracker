import React from 'react';
import styles from './Searchbar.module.css';

const searchbar = () => (
    <input className={styles.Input} type="text" onInput={} placeholder="Search.."></input>
);

export default searchbar;
